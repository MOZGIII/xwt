[package]
name = "xwt-dyn"
version = "0.4.0"
edition = "2021"
resolver = "2"
rust-version = "1.81"
license = "MIT"
description = """
A suite of dyn-safe companion traits for xwt-core.
"""
repository = "https://github.com/MOZGIII/xwt"

[features]
default = ["std"]

alloc = ["xwt-core/alloc"]
std = ["alloc"]

[dependencies]
xwt-core = { version = "0.10", path = "../xwt-core", default-features = false }

dyn_safe = "0.1"
